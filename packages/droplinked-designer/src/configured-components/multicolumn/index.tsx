import { multiColumnCategory } from "./categoryConfig";
import { multiColumnConfig } from "./multiColumnConfig";

export { multiColumnCategory };

export const multiColumnComponents = {
    multiColumn: multiColumnConfig,
}; 