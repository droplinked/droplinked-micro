import { createTextConfig } from "../commonTextConfig";

export const captionConfig = createTextConfig({
    label: "Caption",
    visualExampleFontSize: 16,
    visualExampleFontWeight: "normal",
    defaultFontSize: "16",
    defaultFontWeight: "normal",
}); 