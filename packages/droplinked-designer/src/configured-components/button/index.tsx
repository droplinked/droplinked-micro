import { buttonConfig } from "./buttonConfig";

export { buttonCategoryConfig } from "./categoryConfig";

export const buttonComponents = {
    outlineButton: buttonConfig("outline"),
    secondaryButton: buttonConfig("secondary"),
    normalButton: buttonConfig("default"),
};