import { Config } from "../../puck-editor";

export const buttonCategoryConfig: Config["categories"] = {
    button: {
        title: "Button",
        components: ["outlineButton", "secondaryButton", "normalButton"],
    }
};
